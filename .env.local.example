# Public Supabase project URL (Project Settings → API). Safe to expose in the browser.
NEXT_PUBLIC_SUPABASE_URL=

# Public anon (publishable) key (Project Settings → API). Safe with RLS; never use service_role here.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
